/**
 * Zod schema definitions that:
 * - Validate data structures at runtime
 * - Ensure data integrity
 * - Provide type inference
 * - Define expected data shapes
 */

import { z } from "zod";
// Rest of the file remains exactly the same...